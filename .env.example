# Скопируй в .env для локального запуска (в GitHub эти же имена задаются как Secrets).
# Загрузить локально: set -a && source .env && set +a

CHANNEL_BOT_TOKEN=
CHANNEL_ID=
SERVICE_BOT_TOKEN=
ADMIN_CHAT_ID=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free
POSTS_PER_DAY=5
LOOKBACK_HOURS=24
MAX_CANDIDATES=60
